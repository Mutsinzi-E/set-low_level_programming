# set-low_level_programming
This project introduces the basics of C programming and compilation.
In this task, we create a script named 0-preprocessor that runs a C file stored in the environment variable CFILE through the C preprocessor and saves the output into a file named c.
