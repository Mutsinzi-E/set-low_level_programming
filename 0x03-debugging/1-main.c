#include "main.h"

/**
 * main - tests function that prints the largest of 2 integers
 * Return: 0
 */
int main(void)
{
        int a, b;

        a = 10;
        b = 20;
        largest_number(a, b);

        return (0);
}
