#include "main.h"

/**
 * main - tests function that prints if integer is positive, negative or zero
 * Return: 0
 */
int main(void)
{
    int i;

    i = 0;  /* Test zero case */
    positive_or_negative(i);

    return (0);
}
